# HR 招募總覽看板 - 板規與流程

## 📋 看板目的

**總覽看板**是 HR 招募系統的中央儀表板，用於：
- 即時追蹤所有職缺與候選人狀態
- 視覺化呈現招募 Pipeline
- 提醒需要跟進的案件
- 統計招募成效與數據

## 🗂️ 資料來源

### 1. 職缺資料
- **來源**：`~/clawd/data/headhunter/jobs.json`
- **對應 Google Sheet**：[AIJob 職缺管理](https://docs.google.com/spreadsheets/d/1QPaeOm-slNVFCeM8Q3gg3DawKjzp2tYwyfquvdHlZFE)
- **同步方式**：透過 `jd-manager.sh` 腳本雙向同步

### 2. 候選人資料
- **來源**：`~/clawd/data/headhunter/candidates.json`
- **對應 Google Sheet**：[履歷池索引](https://docs.google.com/spreadsheets/d/1PunpaDAFBPBL_I76AiRYGXKaXDZvMl1c262SEtxRk6Q)
- **同步方式**：透過 `resume-pool.sh` 腳本雙向同步

### 3. 看板輸出
- **本地檔案**：`~/clawd/data/headhunter/dashboard.json`
- **生成工具**：`~/clawd/skills/headhunter/scripts/dashboard.py`

---

## 🔄 Pipeline 階段定義

| 階段代碼 | 階段名稱 | 說明 | 觸發條件 |
|---------|---------|------|---------|
| `pool` | 📋 履歷池 | 候選人進入系統，尚未匹配職缺 | 履歷上傳完成 |
| `new` | 🆕 新進件 | 新履歷待審核 | 剛收到履歷 |
| `matching` | 🔍 匹配中 | 正在與職缺進行匹配 | 手動/自動匹配 |
| `recommended` | 📤 已推薦 | 已向客戶推薦 | 發送推薦信 |
| `interview` | 🎤 面試中 | 客戶安排面試 | 客戶回覆面試時間 |
| `offer` | 💰 Offer中 | 客戶發出 Offer | 收到 Offer 通知 |
| `placed` | ✅ 已報到 | 候選人成功到職 | 確認到職日期 |
| `closed` | ❌ 已結案 | 案件結束（成交或流失） | 手動標記結案 |

---

## 📊 看板數據項目

### 1. 總覽區 (Summary)
- **總職缺數** (`total_jobs`)：所有職缺數量
- **活躍職缺** (`active_jobs`)：狀態為 "active" 的職缺
- **總候選人數** (`total_candidates`)：系統中所有候選人
- **履歷池人數** (`in_pool`)：狀態為 "pool" 的候選人
- **進行中案件** (`in_process`)：處於 matching/recommended/interview/offer 階段的候選人
- **已成交數** (`placed`)：成功到職的候選人數

### 2. Pipeline 區
- 各階段候選人數量統計
- 視覺化長條圖（CLI 版本）
- 百分比分布（可選）

### 3. 提醒區
- **需跟進案件** (`need_followup`)：推薦後超過 3 天未更新的候選人
- **最近動態** (`recent_activity`)：最近 5 筆更新記錄

---

## 🛠️ 操作指令

### Web 介面（推薦）
```bash
# 一鍵啟動
~/clawd/hr-tools/start-dashboard.sh

# 或手動啟動
cd ~/clawd/projects/hr-dashboard
npm run dev
```

**訪問位址**：
- 本地：http://localhost:3000
- 網路：http://[您的IP]:3000

### 生成看板（CLI 版本）
```bash
python3 ~/clawd/skills/headhunter/scripts/dashboard.py
```

### 生成 Telegram 格式
```bash
python3 ~/clawd/skills/headhunter/scripts/dashboard.py telegram
```

### 新增職缺
```bash
python3 ~/clawd/skills/headhunter/scripts/dashboard.py add-job "公司名" "職位名"
```

### 新增候選人
```bash
python3 ~/clawd/skills/headhunter/scripts/dashboard.py add-candidate "候選人姓名"
```

### 更新候選人狀態
```bash
python3 ~/clawd/skills/headhunter/scripts/dashboard.py update CAN-001 recommended
```

---

## 🤖 AI 助理整合規則

### 自動更新時機
1. **履歷進件時**：新候選人自動進入 `pool` 狀態
2. **職缺發布時**：新職缺加入 `jobs.json`
3. **狀態變更時**：候選人狀態改變時更新 Pipeline
4. **每日定時**：每天早上 9:00 和下午 18:00 自動生成看板

### Telegram 通知規則
- **即時通知**：需跟進案件超過 5 件時
- **每日彙報**：每天 21:00 發送當日看板
- **里程碑通知**：候選人進入 `interview`、`offer`、`placed` 階段時

### YuQi 助理指令
在 Telegram 群組中可以使用：
- `看板` 或 `dashboard` → 顯示總覽看板
- `更新看板` → 強制重新生成看板
- `跟進清單` → 顯示需要跟進的案件

---

## 📈 數據計算邏輯

### 進行中案件計算
```
in_process = matching + recommended + interview + offer
```

### 轉換率計算（未來功能）
```
轉換率 = (placed / total_candidates) × 100%
面試轉換率 = (placed / interview) × 100%
```

### 需跟進條件
```
status == "recommended" AND 
updated_at < (now - 3 days)
```

---

## 🔗 Google Sheets 同步

### 同步頻率
- **職缺管理表**：每次新增/修改職缺時同步
- **履歷池索引**：每次履歷上傳時同步
- **看板數據**：不直接同步（按需生成）

### 同步方向
- `jd-manager.sh` → 雙向同步（本地 ↔ Google Sheets）
- `resume-pool.sh` → 單向寫入（本地 → Google Sheets）
- `dashboard.py` → 單向讀取（本地 JSON → 看板）

---

## 🎯 使用場景

### 場景 1：每日站立會議
```bash
# 生成當日看板
python3 ~/clawd/skills/headhunter/scripts/dashboard.py

# 查看需跟進清單
grep "need_followup" ~/clawd/data/headhunter/dashboard.json
```

### 場景 2：快速查詢
```bash
# 查看活躍職缺數
cat ~/clawd/data/headhunter/dashboard.json | jq '.summary.active_jobs'

# 查看 Pipeline 分布
cat ~/clawd/data/headhunter/dashboard.json | jq '.pipeline'
```

### 場景 3：Telegram 報告
在群組中 @YuQi0923_bot，輸入：
```
生成看板
```

---

## ⚠️ 注意事項

1. **資料一致性**：確保 `jobs.json` 和 `candidates.json` 與 Google Sheets 保持同步
2. **狀態更新**：務必在候選人狀態變更時同步更新，避免看板數據失真
3. **跟進提醒**：每天檢查 `need_followup`，確保不遺漏重要案件
4. **歷史記錄**：每次狀態變更都記錄到候選人的 `history` 欄位

---

## 📌 快速參考

| 操作 | 指令 |
|-----|------|
| 生成看板 | `python3 dashboard.py` |
| Telegram 格式 | `python3 dashboard.py telegram` |
| 新增職缺 | `python3 dashboard.py add-job "公司" "職位"` |
| 新增候選人 | `python3 dashboard.py add-candidate "姓名"` |
| 更新狀態 | `python3 dashboard.py update CAN-ID 新狀態` |

---

## 🔮 未來擴展

- [ ] Web 介面版本（Next.js + TailwindCSS）
- [ ] 自動化報表郵件
- [ ] 職缺與候選人智能匹配
- [ ] 客戶滿意度追蹤
- [ ] 佣金計算與財務報表

---

**版本**：1.0.0  
**更新日期**：2026-02-10  
**維護者**：YuQi 🦞 (HR AI 助理)
